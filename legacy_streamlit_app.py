"""
Legacy Streamlit prototype for FinCoach.

This file is the original Streamlit single-file prototype that was
used during early UI exploration. The project has been converted to a
Django backend for iOS clients; the prototype is kept here for
reference and future prototyping work.

To run the legacy Streamlit prototype (if you still want to view it):

    pip install -r requirements.txt
    streamlit run legacy_streamlit_app.py

"""

from pathlib import Path
print("This is the legacy Streamlit prototype. See app.py for the new backend instructions.")
